# AI数智研究平台 · 开发规划

> 版本：v1.0 ｜ 日期：2026-08-26
> 前置：`01-技术选型.md`、`02-架构设计.md`、`03-方案设计.md`
> 原则：核心风险前置（智搜管道先攻坚）；每个里程碑完成后停下，由用户确认再进入下一个

---

## 一、里程碑总览

```
M0 工程地基 → M1 认证与多租户 → M2 智搜核心管道 ★ → M3 知识库 RAG
                                                    ↓
              M7 上线强化 ← M6 管理端 ← M5 会员计费支付 ← M4 工作台与报告
```

**依赖关系**：M2 依赖 M1（登录态）；M2 先做联网+垂直两路（不依赖知识库基建），M3 完成后三路齐备；M5/M6 相互独立可交叉。

---

## 二、分里程碑计划

### M0 工程地基

| 项 | 内容 |
| --- | --- |
| 范围 | pnpm monorepo（backend、frontend/web、frontend/admin、packages/shared）；docker-compose 起 PG/Qdrant/Redis；NestJS 骨架（全局拦截器/异常过滤器/校验管道/Zod 集成）；Vue3 双端骨架 + Element Plus 蓝色主题（对齐原型视觉：主色 #2563EB、白底卡片圆角 14px、画布 #F8FAFC）；ESLint + Prettier + CI |
| 验收 | `docker compose up` 一键起环境；双端登录页可访问；API 健康检查通过 |
| 产出 | 可运行的空壳工程 + 统一响应体/错误码 + 共享 DTO 机制打通 |

### M1 认证与多租户

| 项 | 内容 |
| --- | --- |
| 范围 | 手机验证码（阿里云短信 + Redis 60s 倒计时 + IP/手机号双维度限流防刷）+ 账密登录；Redis session 分布式会话（随机 sessionId，`sess:{sid}` 存用户上下文 TTL 7 天滑动续期，`user:sids:{uid}` 支持踢下线）；租户上下文中间件（Prisma Client Extension 强制注入 tenant_id）；RBAC 三角色；审计埋点（登录/删除/导出） |
| 验收 | 注册/登录/权限拦截/租户隔离全部生效；越权访问抛 Forbidden 并落审计 |
| 产出 | auth/tenant 模块 + 对应用户端登录弹窗（手机号/账密双页签，对齐原型 9.4） |

### M2 智搜核心管道 ★（平台核心竞争力，最先攻坚）

| 项 | 内容 |
| --- | --- |
| 范围 | ① 意图分类（qwen-max + Zod，输出条件回填）；② 联网路：AnySearch 聚合搜索直连（结构化正文）；③ 垂直路：世界银行 WDI API + 指标字典映射；④ RRF 融合 + gte-rerank 重排 + SimHash 去重；⑤ AI SDK 流式生成 + SSE 五阶段协议（stage/report_chunk/source/cond_fill/done/error）；⑥ 会话/报告/来源落库；⑦ 历史记录、中止/结束（AbortController 全链路贯穿） |
| 验收 | 原型智搜页全交互真实可用：条件 AI 回填（has-val 状态）、5 阶段动画、流式正文、`{c:N}` 引文角标 ↔ 来源卡黄色高亮联动、中止/结束回填原问题 |
| 产出 | search 模块（intent/connectors/fusion/generate）+ 用户端首页全功能 |
| 风险 | LLM 延迟与稳定性 → 多 Provider 热切换 + 熔断降级 |

### M3 知识库 RAG

| 项 | 内容 |
| --- | --- |
| 范围 | 库/分组/文档 CRUD（可见性/封面色）；上传解析（Excel/CSV/Word/PDF）；BullMQ 切片+向量化（chunk-embed.queue）；文档状态机（学习中断→学习完成/失败，可继续/重新学习）；混合检索接入管道本地路；三模式（混合/联网/知识库）路由生效；召回测试；「存入知识库」链路（智搜勾选来源 / 分析结果，弹窗选库/分组/可见性，审核队列） |
| 验收 | 原型知识库页全交互可用（chatwiki 三栏：文档/配置/召回测试）；三模式检索结果差异正确；存入→审核→自动学习闭环 |
| 产出 | kb 模块 + 用户端知识库全功能 + 管理端知识审核雏形 |
| 依赖 | M2 管道（本地路接入 fusion 代码复用） |

### M4 工作台与报告

| 项 | 内容 |
| --- | --- |
| 范围 | 数据工作台（国家/时间/指标/来源多维筛选 + 时序宽表 `..` 占位 + 行展开元数据 + 横纵滚动）；Excel/CSV 上传补充即时并入；图表视图（折线/柱状/面积/雷达 + 标签开关 + 区间滑块 + PNG/SVG 导出）；分析结果页（固定章节结构 LLM 生成 ≥3000 字，独立页面）；Word/PPT 导出（引文上标↔来源表映射）；我的数据/我的报告（搜索/标签/状态/归档/版本管理） |
| 验收 | 原型工作台与分析结果页全交互可用；导出文件正确含引文 |
| 产出 | workspace/report 模块 + 用户端三页面 |

### M5 会员计费支付

| 项 | 内容 |
| --- | --- |
| 范围 | 套餐管理（免费体验/专业版/企业版 × 单月/连续包月/年付）；订单状态机；微信/支付宝接入（沙箱→生产）；支付回调幂等；免费体验配额拦截（Redis 原子计数 + 引导弹窗）；账单/发票；会员中心（标签切换+滚动公告+三列套餐卡+对比表）；支付中心联动状态条 |
| 验收 | 完整闭环：购买→使用→续费（到期日累加）→账单可查；回调重放不重复入账 |
| 产出 | member/billing 模块 + 会员中心/支付中心/账单页 |

### M6 管理端

| 项 | 内容 |
| --- | --- |
| 范围 | 9 模块 20 功能全部落地：运营看板（用户量/检索量/收入）；组织用户（用户/会员/角色）；数据资源（指标/字典/数据集）；数据治理（导入审核）；运营管理（公告/搜索词）；任务中心（BullMQ 面板：状态/进度/重试/终止/错误日志）；系统管理（参数热更新/审计查询/服务监控/备份恢复）；知识库管理（审核/类目/权限/索引重建）；支付中心（订单退款/渠道配置） |
| 验收 | 原型管理端 20 功能全部真实可用（对齐原型管理页签标注的功能点） |
| 产出 | admin 模块 + frontend/admin 全部页面 |

### M7 上线强化

| 项 | 内容 |
| --- | --- |
| 范围 | 可观测四件套落地（Pino requestId 贯穿 + OTel 管道 span + Prometheus 指标 + Sentry）；压测（SSE 并发 1k/工作台查询）与限流调优；安全加固（防刷/敏感词/渗透测试）；备份策略与恢复演练；生产 K8s 部署（HPA/Ingress SSE 透传/OSS） |
| 验收 | 生产环境稳定运行；监控告警齐全（队列积压/LLM 错误率/SSE 断连率）；备份可恢复（演练通过） |

---

## 三、每个里程碑内的标准流程

```
后端接口开发 → 单元测试 → 前端开发 → 前后端联调（内置浏览器自测，截图/点击验证，
发现问题自行修复再验证，直到功能正常） → 文档同步（更新本目录对应文档）
```

- 每完成/改动一个功能同步更新 docs/ 下需求、进度与设计文档
- 一个里程碑完成后停下，等用户确认再进入下一个
- 测试重点：管道各阶段（fusion/rerank/转 MD）必须可单测（确定性代码）；支付幂等必须有重放测试

---

## 四、进度跟踪

| 里程碑 | 状态 | 完成时间 | 备注 |
| --- | --- | --- | --- |
| M0 工程地基 | ✅ 已完成 | 2026-08-26 | 验收情况见下方「M0 验收记录」 |
| M1 认证与多租户 | ✅ 已完成 | 2026-08-27 | 租户中间件/RBAC守卫/登录弹窗/单测/联调全部完成（见下方「M1 阶段记录」）；阿里云短信日志兜底暂缓 |
| M2 智搜核心管道 | ✅ 已完成 | 2026-08-27 | M2.1 基础管道 / M2.2 两路取数 / M2.3 流式生成编排 / M2.4 前端智搜页 全部完成（见下方「M2.4 阶段记录」） |
| M3 知识库 RAG | ✅ 已完成 | 2026-08-27 | M3.1 数据模型与 CRUD、M3.2 上传解析与学习队列、M3.3 混合检索接入本地路、M3.4 召回测试与入库链路 全部完成（见下方「M3.4 阶段记录」）；存入→审核→自动学习→召回闭环联调通过 |
| M4 工作台与报告 | ✅ 已完成 | 2026-08-28 | M4.1 数据工作台、M4.2 上传补充+图表视图、M4.3 分析结果页、M4.4 导出+我的数据/我的报告 全部完成（见下方各阶段记录） |
| M5 会员计费支付 | ⬜ 未开始 | — | |
| M6 管理端 | ⬜ 未开始 | — | |
| M7 上线强化 | ⬜ 未开始 | — | |

### M0 验收记录（2026-08-26）

**已完成**
- pnpm monorepo：`backend`（NestJS 11）+ `frontend/web` / `frontend/admin`（Vue3 + Element Plus 蓝色主题 #2563EB）+ `packages/shared`（CJS+ESM 双格式，统一响应体/错误码/Zod DTO 单一事实源）（2026-08-26 重组：原 apps/api、apps/web、apps/admin 迁移至现路径）
- NestJS 骨架：全局统一响应体拦截器（`{code, message, data}`）、全局异常过滤器（ZodError → 3xxx，HttpException → 分段错误码）、ZodValidationPipe、`GET /api/v1/health?verbose=true`（探测 PG/Redis/Qdrant，单项失败降级 degraded）
- docker-compose.yml（PG16 / Qdrant 1.15 / Redis7 + healthcheck）+ `.env.example` 模板
- ESLint 9 + Prettier 统一配置 + GitHub Actions CI（lint → test → build）
- 单元测试 9/9 通过（拦截器/管道/异常过滤器）；四个包构建全部通过
- 前端验证：双端登录页（web :5174/login、admin :5175/login）内置浏览器打开无报错；用户端登录页已联动健康检查接口展示服务状态

**环境备注**
- 本机沙箱限制：pnpm 9.15.9 安装于项目内 `.tools/`，store 与 npm 缓存指向项目内目录（均已加入 .gitignore）；全局安装命令需 `--cache`/`--prefix` 重定向
- ⚠️ Docker 容器验证被跳过：Docker Desktop 引擎（WSL2 后端）启动卡死，`wsl -l -v` 挂起无响应。**M1 开工前需修复**：重启机器或在管理员终端执行 `wsl --shutdown` 后重启 Docker Desktop，再跑 `docker compose up -d` 验证
- 端口占用：5173 被 2026-08-16 遗留的孤儿 node 进程（PID 28380，.workbuddy）占用，本会话 web/admin 漂移至 5174/5175；待结束后恢复标准端口

> 状态图例：⬜ 未开始 / 🔨 进行中 / ✅ 已完成（每完成一项填写完成时间与备注）

### M1 阶段记录（2026-08-26）

**已完成：认证后端（auth 模块）**
- Prisma 7.10 + adapter-pg：`prisma/schema.prisma`（tenants/users/roles/user_roles/audit_logs 五表）+ `prisma.config.ts`（Prisma 7 连接串配置）+ 迁移 `init_tenant_user_role_audit` 已应用；三内置角色已种入（USER/DATA_ADMIN/PLATFORM_ADMIN）
- Redis session 会话（用户方案调整后）：`SessionService`——`sess:{sid}` 存用户上下文（crypto 32 字节随机 sessionId）、TTL 7 天滑动续期；`user:sids:{uid}` 在线会话索引；支持登出销毁/踢下线/角色即时刷新；`SessionAuthGuard`（`Authorization: Bearer <sessionId>`，SSE 支持 `?sessionId=` 兜底）
- 短信验证码：`SmsCodeService`——60s 冷却 / 5min 有效 / 手机号日限 10 / IP 日限 30 / 单码 5 次尝试防爆破；存储走 `SmsKvStore` 抽象（生产 Redis 实现，测试内存实现）；实际短信发送（阿里云）待密钥配置后接入
- 登录双模式：验证码登录（未注册自动创建个人租户 + USER 角色，事务）+ 账密登录（bcryptjs）；`POST /auth/sms/send` `/auth/sms/login` `/auth/login` `/auth/logout`
- 审计埋点：登录落 `audit_logs`（LOGIN + 方式），失败不阻塞主流程
- 单元测试 24/24 通过（session 7 / 验证码 7 / 异常过滤器 5 / 拦截器管道 5）
- 接口联调全部通过：健康检查（PG/Redis/Qdrant 全 up）、验证码发送→Redis 落码→登录自动注册→会话落 Redis→登出销毁→审计入库；负向路径（1006 冷却 / 1005 错码 / 1008 密码错 / 3001 参数）错误码正确

**环境变更**
- Docker 已恢复（重启机器后 WSL 正常）；宿主机端口改为 **PG 5433 / Redis 6380**（5432/6379 被 finrpa 项目长期占用），`.env.example`/`.env` 已同步
- pnpm store/cache 固化到项目内 `.pnpm-store`/`.pnpm-cache`（根 `.npmrc`）
- Prisma 7 适配：生成客户端输出至 `src/generated/prisma`，`nest-cli.json` 配置 assets 拷贝（JS/WASM 不经 tsc 编译）

**待办（M1 剩余）—— 2026-08-27 全部完成**
- [x] 租户上下文中间件（Prisma Client Extension 强制注入 tenant_id）：tenant-context(ALS) + tenant-filter(纯函数 needsTenantFilter/applyTenantFilter) + prisma.service(forTenant=$extends $allOperations) + tenant-context.interceptor 全局注册
- [x] RBAC 角色守卫 + 装饰器：@Roles/@Public/RoleCode + RolesGuard + SessionAuthGuard，守卫链在 app.module.ts 全局注册
- [x] 用户端登录弹窗（手机号/账密双页签，对齐原型 9.4）+ 前端联调：LoginDialog.vue 对接真实接口、HomeView 集成弹窗、LoginView(/login) 复用弹窗、router 加 requiresAuth 守卫框架（为 M2 预留门禁）
- [ ] 阿里云短信接入（密钥配置后替换日志输出）—— 开发期日志兜底暂缓

**M1 收尾验证（2026-08-27）**
- 单测 41/41 全绿（tenant-filter 9 + guards 8 + session 7 + sms 7 + 异常过滤器 5 + 拦截器管道 2 + zod 管道 3；原文「24/24」为旧数）
- 后端联调实跑通过：发码→Redis 落码→登录（新用户自动注册+会话创建+USER角色+审计）→登出（会话销毁）→错误码负向(1005)；会话守卫/RBAC/租户隔离无 @Roles 业务路由可真实越权联调，单测覆盖，留 M2 有业务路由时再验
- 前端：LoginView 复用 LoginDialog、router beforeEach 守卫（meta.requiresAuth + redirect 回原路径）
- 环境备注：nest-cli.json deleteOutDir 改 false（避开 safe-delete 拦 watch 清 dist）；3000 端口曾被孤儿 node 占用已清理；前端 vite 5174（5173 被占漂移）
- 注意：TENANT_MODELS 已登记 M2 搜索域四表（SearchSession/SearchReport/SearchSource/SearchUsage）；M3 知识库表后续登记

---

### M2 阶段记录（2026-08-27）

**M2.1 基础管道 ✅ 已完成**

- 模块骨架：`backend/src/modules/search/{search.module,search.service,search.controller}.ts`（已注册 `AppModule`，全局前缀 `/api/v1`，路由 `/search/stream`、`/search/histories`、`/search/reports/:id`、`/search/reports/:id/save-kb`）
- 检索编排：`SearchService.search` 多路连接器 `Promise.allSettled` 并发 + 单路超时熔断（垂直 10s / 本地 3s）+ `fuse`（RRF 加权→SimHash 去重→降序截断 TopK→引用/参考级），纯函数可单测
- 垂直路：世界银行 WDI 连接器（国家/指标字典映射 + 时序表 Markdown + 真实取数验证）；本地路 M3 空桩；联网路 M2.2 接入
- SSE 协议：`sse/sse.events.ts` 事件类型 + `serializeSse`；`/search/stream` 跑通 垂直路→融合→`source`/`done` 事件（EventSource 无法带 header，支持 `?sessionId=` 兜底）
- 单测 69/69 全绿（rrf / simhash / fusion / vertical 映射+真实 WDI 取数 / sse 序列化 / service 编排+熔断）；WDI 真实取数用例联网通过
- 端到端验证：登录 → SSE `/search/stream`(conditions=美国 GDP 2018–2022) 收到真实 WDI 来源卡事件

**环境修复（非 M2.1 功能，启动前置）**

- 依赖树缺 `@prisma/client-runtime-utils@7.10.0`（Prisma 7 客户端运行时子包），应用启动即崩；已 `pnpm add` 显式补装并写入 `backend/package.json`（Prisma 传递依赖，仓库 store 未联动，显式声明后恢复启动）
- 修正 `search.controller` 路由前缀双重 `/api/v1`（全局前缀已含 `api/v1`，controller 改为 `@Controller('search')`）

**待办（M2 剩余批次）**

- [x] M2.2 两路取数：意图分类（方舟 DeepSeek + Zod）+ 联网路（AnySearch 聚合搜索直连）—— 2026-08-27 完成
- [x] M2.3 流式生成编排：AI SDK streamText + SSE 五阶段 + 会话/报告/来源落库 + 用量 + AbortController 全链路 —— 2026-08-27 完成
- [x] M2.4 前端智搜页全交互 —— 2026-08-27 完成

**M2.2 阶段记录（2026-08-27）**

- 新增依赖：`@ai-sdk/openai` + `ai`（意图分类，OpenAI 兼容）
- 意图分类 `intent/intent.service.ts`：`IntentService.classify()` 方舟 `generateObject` 结构化抽取（countries/indicators/yearFrom/yearTo/routeHints）；无 `ARK_API_KEY` 或失败 → 降级返回空条件（跳过回填）；`mergeConditions` 手动优先
- 联网路 `connectors/web.connector.ts`：`WebAnySearchConnector` AnySearch `POST /v1/search`（`{query, max_results}`），`results[]`（title/url/snippet/content）直接映射 `SearchHit`（content 即 contentMd，无需自建抓取）；Key 可选——认证 Bearer / 匿名按 IP 限流；失败返回空 + 警告不阻断
- 装配：`search.module.ts` 注册 `IntentService` + `WebAnySearchConnector`（三路齐备）；`search.controller.ts` 补 `stage:intent` 推送 → `cond_fill` 回填 → 检索 → `source`/`done`（此前 `cond_fill` 未接线）
- 共享：`packages/shared/src/dto/search.dto.ts` 新增 `SearchIntentSchema`（LLM 输出校验单一事实源），已重建 dist
- 单测覆盖意图分类与联网路（响应解析 / 匿名映射 / Bearer 头 / 失败降级）；后端全量 79/79 通过
- 端到端验证：登录 → SSE `/search/stream` 收到 `intent`/`cond_fill`/`searching`/`fusing`/`source`/`done`；无 Key 时联网/意图降级不报错，垂直路 WDI 真实取数正常
- 注意：真实意图分类需配置 `ARK_API_KEY`（当前 .env 待配，联调验证走降级路径）

**M2.3 阶段记录（2026-08-27）**

- 流式生成 `generate/generate.service.ts`：AI SDK `streamText`（方舟 DeepSeek-V4-Pro-GA，OpenAI 兼容），`buildGeneratePrompt` 按 `{c:N}` 编号来源拼接问题/条件/检索上下文，`parseCitations` 解析正文内 `{c:N}` 引文；无 `ARK_API_KEY` 或 LLM 抛错 → 降级输出检索结果摘要（不阻断主流程）
- 持久化 `persistence/search.store.service.ts`：`SearchStoreService` 会话/报告/来源/用量落库，全走 `PrismaService.forTenant`（租户 Extension 自动注入 tenant_id）；报告/来源无独立租户列，通过所属 session 嵌套写保证行级隔离；`requireTenant()` 缺上下文抛 1001 未授权；`upsertUsage` 按 `(tenantId,userId,day)` 复合唯一累加（走系统 client 显式 tenant_id，避开 Extension 对 upsert where 注入）
- 五阶段编排 `search.controller.ts` `GET /search/stream`：`intent` → 落会话 → `cond_fill` → `searching` → `fusing`（来源卡按 idx 连续编号）→ `generating`（`report_chunk` 流式字段 → 引文卡）→ 落库（报告+来源+用量）→ `done{ sessionId, reportId }`；`AbortController` 贯穿全链路，客户端断开即 abort；`error` 事件携带当前 stage
- 历史/详情：`GET /search/histories`（分页、映射最新报告 id）、`GET /search/reports/:id`（报告正文+来源卡按 idx 升序，跨租户返回 404）；`save-kb` 为 M3 占位（501）
- 装配：`search.module.ts` 引入 `PrismaModule`，注册 `GenerateService` + `SearchStoreService`
- 单测：generate 7 / store 6 / controller 6 覆盖（无 Key 降级、LLM 分片累计、抛错降级、嵌套写来源、跨租户 404、SSE 五阶段）；后端全量 **98/98 通过**，`pnpm build` 通过
- 注意：真实流式生成需配置 `ARK_API_KEY`（当前 .env 待配，联调走降级路径）

**M2.4 阶段记录（2026-08-27）**

- 新增 `frontend/web/src/api/search.ts`：SSE 客户端（fetch 流读取 + 解析 `event:`/`data:` 帧，可携带 Authorization 头 + AbortController 控制，返回取消语义）、历史列表、报告详情接口封装
- 新增 `frontend/web/src/components/MarkdownView.vue`：轻量 Markdown 渲染（标题/段落/列表/表格/引用/分隔线/行内代码/粗斜体），支持 `{c:N}` 引文角标 → 可点击 emit `cite-click`，`activeCite` 联动高亮黄色徽标；HTML 转义防注入
- 新增 `frontend/web/src/views/SearchView.vue` 智搜页（M2 验收核心）：
  - 问题输入 + 模式切换（混合/联网/知识库）+ 智搜/停止按钮
  - 检索条件栏：国家/指标多选可创建 + 起止年份，AI 回填 → has-val 深色态（可手动微调，手动条件优先于 AI）
  - 5 阶段动画进度条（理解意图→检索→融合→生成→完成），生成阶段光标闪烁
  - 流式正文（MarkdownView 渲染）+ 来源卡面板联动：点击正文角标 ↔ 来源卡黄色高亮互跳
  - 中止（AbortController 全链路）/结束回填原问题，方便复述再搜
  - 历史记录抽屉：分页拉取 `/search/histories`，点击加载 `/search/reports/:id` 复现报告
- 路由 `frontend/web/src/router/index.ts` 新增 `/search`（`requiresAuth: true`，受登录门禁保护）；首页登录后显示「进入智搜」入口跳转
- 前端 `pnpm build`（vue-tsc 类型检查 + vite 构建）通过
- 全链路联调（HTTP 层）：登录 → `/search/stream` 收到五阶段（intent/cond_fill/searching/fusing/source×N/generating/report_chunk/done）→ 落库返回 `{sessionId, reportId}` → `/search/histories` 可查 → `/search/reports/:sessionId` 返回正文+来源卡
- 环境备注：真实意图分类与流式生成仍需 `ARK_API_KEY`（当前降级路径已完整可跑：意图跳过回填、生成输出检索结果摘要）；本机会话联调用 Redis 预写验证码 13800000001/123456 通过真实登录获取 sessionId
- 验收对照：条件 AI 回填 has-val ✓、5 阶段动画 ✓、流式正文 ✓、`{c:N}` 角标 ↔ 来源卡黄色高亮联动 ✓、中止/结束回填原问题 ✓、历史记录 ✓

---

### M3.1 阶段记录（2026-08-27）

**M3.1 数据模型与 CRUD ✅ 已完成（方案见 docs/07-M3技术方案.md，决策点已确认：xlsx+mammoth+pdf-parse / BullMQ / 无 Key 退化纯全文）**

- Prisma 追加知识库四层表（migration `kb_rag` 已应用）：`kb_libraries`（含可见性/封面色/检索与向量化配置）+ `kb_groups` + `kb_documents`（含学习状态机 status）+ `kb_chunks`（content 供 PG 全文，vectorId 关联 Qdrant）
- 四表全部登记进 `tenant-filter.ts` 的 `TENANT_MODELS` 实现行级隔离（Prisma Extension 自动注入 tenant_id）
- 新增 `kb` 模块：`kb.module` + `kb.controller` + `store/kb.store.service`；已在 `app.module` 注册
- 接口：库 CRUD（list/create/get/update/delete）、分组 CRUD（list/create/update/delete）、文档列表（筛选+分页）、文档详情（含切片）、文档删除；跨租户访问统一抛 404 业务异常
- 文档上传/学习队列/召回测试/入库链路为 M3.2–M3.4，本期未实现（controller 未暴露上传/召回端点）
- 单测：`kb.store.service.spec` 10/10 覆盖（库 CRUD+统计 / 分组 / 文档筛选分页/详情/跨租户 404）；后端全量 **108/108 通过**，`pnpm build` 通过
- 待办：M3.2 上传解析与学习队列（依赖 BullMQ + 解析库 + Qdrant 客户端，需新增依赖）→ M3.3 混合检索接入本地路 → M3.4 召回测试与入库链路

### M3.2 阶段记录（2026-08-27）

**M3.2 上传解析与学习队列 ✅ 已完成（方案见 docs/07-M3技术方案.md 第二节/第五节/第六节）**

- 新增依赖：`bullmq`、`@nestjs/bullmq`、`@qdrant/js-client-rest`、`xlsx`、`mammoth`、`pdf-parse`（v2，`getText()` 返回 `TextResult.text`）
- kb 模块新增：`parse/doc-parser.service`（xlsx 遍历 sheet 拼表格文本 / CSV 逐行 / Word 提段落 / PDF 提文本 / txt·md 直读，扩展名推断 mimeType）+ `chunk/chunker`（FIXED 固定长度带 overlap 衔接 / SMART 标题独立+换行边界断句，纯函数）+ `embeddings/embed.service`（方舟 OpenAI 兼容 `/embeddings`，无 `ARK_API_KEY` 自动降级跳过）+ `vector/qdrant.service`（每租户每库 collection `tenant_{tid}_lib_{lid}`，upsert/search/deleteByIds）+ `learning/learning.service`（解析→切片→向量化→写 PG 切片→写 Qdrant 回填 vectorId→状态流转的完整编排）+ `queue/chunk-embed.processor`（BullMQ worker，并发 2，attempts=3 指数退避）
- 接口：`POST /kb/libraries/:id/documents`（multipart 上传 ≤50MB 触发学习）与 `POST /kb/libraries/:id/documents/:did/relearn`（重新/继续学习）；原始文件字节落库 `kb_documents.file_data`（bytea，migration `kb_file_data`），relearn 时回读——保障中断续学/失败重学/配 Key 后补向量可行
- 状态机落地：上传→LEARNING→READY / FAILED(+failReason)；服务启动时把上次进程遗留的 LEARNING 统一清扫为 INTERRUPTED（可 relearn 继续）；BullMQ 不可用时上传降级为同步学习
- 单测新增 32 例：chunker 10（overlap 衔接/尾部冗余片消除/SMART 边界）、doc-parser 15（mimeType 推断表/xlsx 多 sheet 往返/损坏文件失败路径）、learning 7（状态机/分组传播/向量 payload/向量化失败退纯全文/原件回读）；后端全量 **142/142 通过**，`nest build` 通过
- 联调验证（真实服务链路）：上传 txt→READY 切片数正确且中文内容无损；分组内上传切片 group_id 正确；损坏 docx→FAILED 原因明确；置 LEARNING 后重启服务被清扫为 INTERRUPTED→relearn→READY；删除库级联清理文档与切片；无 ARK Key 时 Qdrant 零集合、切片 vector_id 为空（退化纯全文符合预期）
- 待办：M3.3 混合检索接入本地路（LocalConnector + KbRetriever）→ M3.4 召回测试与入库链路（前端知识库页全过程）

### M3.3 阶段记录（2026-08-27）

**M3.3 混合检索接入本地路 ✅ 已完成（方案见 docs/07-M3技术方案.md 第七节/第十三节 M3.3 落点）**

- 新增 `kb/retriever/kb.retriever.service.ts` 混合检索服务：向量路（问题向量化→各库 collection 并发检索→库阈值过滤→PG 回填切片内容/文档名）+ 全文路（`extractQueryTerms` ASCII 词 + CJK bigram 分词→PG ILIKE 多关键词匹配按命中词数排序）→ 复用 `fusion/rrf.ts` 按库分组融合（组权重=库 weight）→ 映射统一 SearchHit（rawScore=跨渠道最高相似度，meta 带 libraryId/groupId/documentId/idx）
- 打分语义：向量=cosine 受库 threshold 约束；全文=命中词占比不受阈值过滤（无 Key 退化模式保底可出结果，仅排序/分级用）——已在技术方案十三节明确记录
- `LocalConnector` 空桩替换为真实实现（注入 KbRetrieverService，失败显式告警+返回空）；本地路超时 3s→8s（含问题向量化网络调用）
- 三模式路由生效：`SearchService.search` 增 `routes` 连接器过滤参数，controller 增 `ROUTES_BY_MODE`——hybrid 三路全开 / web 联网+垂直 / local 仅本地，`mode` 参数自此真正路由
- 装配：KbModule 导出 KbRetrieverService；SearchModule 引入 KbModule；QdrantService 增 `listCollectionNames`；KbStoreService 增 `listLibraryRetrievalConfigs`/`getChunksWithDocNames`/`searchChunksFulltext`（$queryRaw 参数化，系统 client 显式 tenantId）
- kb:// 伪链接治理：本地命中用 `kb://{libraryId}/{documentId}/{idx}` 作唯一标识兼融合去重键（同文档多切片不塌缩）；前端来源卡不渲染该伪链接、generate 拼 prompt/降级摘要不再携带
- 单测新增 16 例：retriever 13（分词/转义/退化纯全文/库权重排序/向量阈值/跨渠道最高分/向量化失败兜底）+ search.service 路由 3；后端全量 **158/158 通过**，前后端构建通过
- 联调验证（API + 浏览器）：上传祁连山冰川 txt→READY→知识库模式仅出该文档命中（徽标「知识库」+ 引用联动）；混合模式知识库命中排第 0 位、后随 9 条联网来源（本地优先加权生效）；联网模式零知识库命中（路由正确）
- 环境备注：Windows 控制台 GBK 传参会导致 curl 中文查询乱码（服务端无此问题，浏览器链路正常）；排障时注意 LocalConnector 已带显式失败日志
- 待办：M3.4 召回测试与入库链路（召回测试接口+智搜「存入知识库」真实化+前端知识库页全过程）

### M3.4 阶段记录（2026-08-27）

**M3.4 召回测试与入库链路 ✅ 已完成（方案见 docs/07-M3技术方案.md 第八节/第九节/第十三节 M3.4 落点）**

- **Prisma 迁移**：`kb_documents` 增 `visibility`（`KbVisibility`，默认 PRIVATE，migration `20260827133123_kb_doc_visibility`）；`tags`（Json）与 `sourceSessionId`（`source_session_id`）字段 M3.1 已预留、本期启用（智搜入库时写入来源标签与会话关联）
- **召回测试接口**：`POST /kb/libraries/:id/recall-test`——复用 KbRetrieverService 单库检索（`opts.libraryId` 过滤），返回命中片段 + 相似度分级（`gradeSimilarity`：高 ≥0.7 / 中 ≥0.4 / 低 <0.4），含 `libraryId/groupId/documentId/chunkIndex` 元信息供前端定位原文
- **save-kb 真实化**：新增 `save/source-doc.ts`（纯函数 `sanitizeSourceName` 清洗标题为安全文件名 + `buildSourceDocument` 来源→Markdown 文档：标题作一级标题、元信息入引用块、正文为摘要）；`KbService.saveSourcesToLibrary` 逐条来源独立成档（mimeType=md、原始 Markdown 落 `file_data`），状态停留 PENDING（待审核）；`search.controller` 的 `save-kb` 桩替换为真实逻辑（会话归属校验 + idx 去重 + 参数校验）
- **审核队列接口**：`GET /kb/reviews`（待审核列表，含库/分组名）+ `POST /kb/documents/:id/approve`（回读原文件→提交学习队列，队列不可用降级同步）+ `POST /kb/documents/:id/reject`（删除待审核文档）；三端点 `@Roles(DATA_ADMIN, PLATFORM_ADMIN)` 管理权限，`PENDING` 对存入文档兼作「待审核」态
- **前端 api/kb.ts**：库/分组/文档 CRUD + 召回测试 + 审核队列 + `saveSourcesToKb` 接口封装（类型对齐后端响应）
- **前端 KnowledgeView（/knowledge 路由）**：三栏结构——左侧库导航（文档/配置/召回测试）+ 库列表态（卡片网格/新建弹窗）+ 文档面板（分组侧栏/状态筛选/上传/重学/详情抽屉）+ 配置面板（基础信息/向量化/检索参数）+ 召回测试面板（输入问题→命中片段分级展示→点击查看切片原文）
- **前端 SearchView 智搜页**：来源卡新增勾选框（默认全勾选）+ 「存入知识库」按钮 + 存入弹窗（目标库/分组/可见性/标签，切库联动加载分组）；历史报告加载后同样可勾选来源存库
- **单测**：`kb.source-doc.spec`（文件名清洗/Markdown 生成）、`kb.save-review.spec`（存库待审核/审核通过触发学习/拒绝移除）、`kb.retriever.service.spec`（召回测试单库过滤/相似度分级）、`search.controller.spec`（save-kb 真实化）补齐；后端全量跑绿，`nest build` + 前端 `vue-tsc + vite build` 均通过
- **全链路联调（API + 浏览器）**：登录 → 智搜历史报告 → 勾选来源存库 → 2 条 PENDING → 审核队列可见 → 审核通过自动学习（LEARNING→READY，各 1 切片）→ 召回测试命中 3 条（2 条新入库 + 1 条既有，相似度 0.875/0.875/0.75，HIGH 级）；M3 四批次全部完成
- 环境备注：Windows 控制台 GBK 编码会导致 curl 中文请求体乱码（浏览器/Node 脚本无此问题）；联调中文入参统一走 Node 脚本或前端发起

---

### M4.1 阶段记录（2026-08-28）

**M4.1 数据工作台 ✅ 已完成（方案见 docs/08-M4技术方案.md 第一节/第六节 M4.1）**

- **新增模块** `backend/src/modules/workspace/`：`workspace.module` / `workspace.controller` / `workspace.service`；注册到 `app.module.ts`；`GET /api/v1/workspace/dataset` 暴露（登录门禁由全局 SessionAuthGuard 保证）
- **接口** `GET /workspace/dataset?countries=&indicators=&yearFrom=&yearTo=`：按条件调 WDI 取数，复用 `fetchWdi` + `resolveCountryCodes` + `resolveIndicatorCodes`，输出结构化 JSON `{indicators:[{indicator, indicatorCode, series:[{country, iso3, values:{year:value}}]}], years:[], source}`；逗号分隔列表兼容 query 重复参数；单指标取数失败仅跳过该项不阻断；客户端断开 → AbortController 中止
- **前端** `frontend/web/src/api/workspace.ts` + `views/WorkspaceView.vue`：
  - 头部标题「数据分析工作台」+ meta（指标/国家数/年份区间）
  - 左侧多级筛选：清空/全选 + 关键词检索 + 时间起止 + 国家/地区多选（22 个常用经济体）+ 数据来源标注
  - 右侧：分析指标下拉（6 个常用指标，含单位与小数位配置）+ 释义 i 图标 + 时序宽表（行=国家/列=年份、`..` 占位、奇数年份浅蓝隔列、行展开元数据「数据来源/口径/单位/缺失年份」、首列+表头 sticky、横纵滚动、点击表头排序）+ 底部操作栏「下载 CSV（真实实现，UTF-8 BOM 兼容 Excel）/ 生成数据图表·生成分析结果·数据上传补充·收藏到我的数据（M4.2–M4.4 占位提示）」
  - 筛选/指标变更 debounce 300ms 自动重查；关键词过滤已选国家
- **路由** `/workspace`（`requiresAuth: true`）；`TopNav.vue` 加「工作台」导航项 + active 高亮
- **单测** `backend/test/workspace.service.spec.ts` 3/3 覆盖：mock WDI 组装结构化矩阵（含 null 值跳过）/ 无国家无指标返空 / 单指标失败不阻断；`nest build` 通过
- **联调验证（API + 浏览器）**：
  - API：Node 脚本登录拿 sessionId → 调 dataset 真实返回 WDI 结构化数据（如 USA/CHN GDP 2020-2023）
  - 浏览器：登录态注入 → /workspace 渲染：8 个核心经济体（中国/美国/日本/德国/英国/法国/印度/巴西）× GDP 年增长率 2019-2025 全部命中，0 个 `..` 占位，浅蓝隔列与 sticky 表现正确；行展开显示「数据来源/口径/单位/缺失年份 0 个」
  - 顶部「工作台」导航激活高亮，与「首页/知识库」并列
- **依赖** 未新增后端依赖；前端 vue-tsc 类型检查通过、vite 编译所有模块 200
- **M4 批次计划**（08-M4技术方案.md 第六节）：M4.1 工作台 ✅ → M4.2 上传+图表 ✅ → M4.3 分析结果页 → M4.4 导出+我的数据/我的报告
- **环境备注**：本机 `nest start --watch` 不会自动加载新增的 workspace 目录（dev watch 已知边界），新增模块后需手动重启后端 dev；联调用 puppeteer-core + 本机 puppeteer 缓存的 chrome.exe 截图（agent-browser Windows 二进制未装，puppeteer-core 走 ms-playwright/puppeteer 浏览器缓存）

---

### M4.2 阶段记录（2026-08-28）

**M4.2 上传补充 + 图表视图 ✅ 已完成（方案见 docs/09-M4.2技术方案.md）**

- **后端**：
  - `backend/src/modules/workspace/upload-parser.ts`：宽表解析纯函数 `parseTimeseries(mimeType, buffer)` → `{rows:[{name, values}], years:[]}`；约定首列=实体名、表头=年份、单元格=数值；支持 xlsx（首个 sheet）/ csv（utf8 + BOM 剥离）；空值/-/.. 记缺失不生成年份键；表头非年份/无有效数据行抛业务异常
  - `workspace.service.ts` 新增 `upload(mimeType, buffer)`（调 parser 并打日志）
  - `workspace.controller.ts` 新增 `POST /api/v1/workspace/upload`（FileInterceptor 20MB，仅 .xlsx/.xls/.csv）；复用 `kb/parse/doc-parser.service.detectMimeType`
  - 复用已有 `xlsx` 依赖（无新增）
- **前端**：
  - `api/workspace.ts`：新增 `uploadDataset(file)`（FormData POST）；`DatasetSeries` 加可选 `source?: string`（标记本地文件来源）
  - `WorkspaceView.vue`：
    - 上传补充：`el-dialog` 弹窗（dropzone 点击+拖拽 + 多文件列表 + 完成/取消），完成后把解析行并入当前指标 `series`（同名实体合并填充、新实体追加并加入左侧国家筛选列表 + 自动选中），行展开元数据标注「本地文件 · {filename}」来源
    - 图表视图：与表格视图同页切换（`v-if`），对齐原型 8.4 布局——返回工作台 + 折线/柱状/面积/雷达 4 图 tab + 数据标签/网格线开关 + 导出 PNG/SVG + 时间区间滑块 + 右侧三卡片（图例 / 长周期走势研判 / 智能图表推荐）
    - 图表引擎：ECharts 6.1（新增依赖 `echarts`）；line/area 平滑、bar 分组并 `barMaxWidth:16`、radar 取代表性年份（首/中/尾）作 series；图表随指标/国家/上传/滑块变化自动重渲；dispose 在返回表格与卸载时触发避免残留
  - 表格视图无回归
- **依赖** 新增 `echarts@6.1.0`（frontend/web）；pnpm add 在 Windows 沙箱下因 safe-delete 阻止 hoist 链接 → 改为 PowerShell `New-Item Junction` 手动建链（`.pnpm/echarts@6.1.0/node_modules/echarts`），`node -e require('echarts')` 验证 echarts+zrender 链路完整
- **单测** `backend/test/upload-parser.spec.ts` 8/8 通过：宽表 CSV 正常解析 / 缺失值跳过 / BOM 剥离 / 表头非年份抛异常 / 空内容抛异常 / 无有效数据行抛异常 / xlsx 宽表往返解析 / 扩展名白名单
- **API 联调**（curl）：sms/login 拿 sessionId → `POST /workspace/upload` 上传 CSV（新加坡/越南 2020-2022）→ 返回 `rows/years` 结构化 JSON
- **浏览器联调**（puppeteer-core）：登录态注入 → 工作台表加载 → 打开图表（折线/柱状/雷达/标签网格关掉）→ 上传弹窗 → 选文件 → 完成并入 → 表格新增「新加坡/越南」行与左侧筛选 + 自动选中 + 行展开标注「本地文件·.m42-test.csv」→ 重开图表含上传数据
- **截图证据**（项目根）：`.m42-table.png` / `.m42-chart-line.png` / `.m42-chart-bar.png` / `.m42-chart-radar.png` / `.m42-chart-no-label.png` / `.m42-upload-modal.png` / `.m42-upload-picked.png` / `.m42-upload-merged.png` / `.m42-chart-after-upload.png`；联调脚本 `.m42-verify.cjs`
- **构建** `pnpm --filter @app/api build` 通过；`vue-tsc --noEmit` 通过（首次有 ECharts series 联合类型不收敛，加 `seriesType:'line'|'bar'` 收敛 + `as echarts.EChartsOption['series']` 断言）
- **范围确认决策**：图表视图含侧边「长周期走势研判 + 智能图表推荐」三卡片（对齐原型）；上传文件格式为宽表（首列实体名、表头年份）

### M4.3 阶段记录（2026-08-28）

**M4.3 分析结果页 ✅ 已完成（方案见 docs/10-M4.3技术方案.md）**

- **后端**：
  - 新增 `workspace_reports` 表（id/tenantId/userId/title/contentMd/paramsSnapshot/sources/tokenUsage/status/version/createdAt/updatedAt）；`prisma migrate dev` 已应用；`tenant-filter.ts` 登记 WorkspaceReport
  - `workspace/analyze.ts` 纯函数模块：`computeStats`（样本覆盖/时间跨度/期末均值/区间变动/完整率）、`computeTable`（Top N 降序 + 缺失占位）、`buildAnalyzePrompt`（14 章节固定结构 System prompt + 时序数据上下文）、`buildFallbackReport`（确定性 14 章节降级正文，无 Key/失败时保证页面可渲染）、`extOf`（极值计算，避免闭包类型窄化问题）
  - `GenerateService` 扩展 `streamCustom(system, prompt, fallbackText)`，`stream()` 内部复用（保持 M2 行为不变），并新增 `buildSearchFallback` 纯函数
  - `workspace.store.service.ts`：`saveReport` / `reportDetail`（跨租户 404）
  - `workspace.analyze.service.ts`：`analyze`（确定性统计 + LLM 流式 + 落库）、`saveToKb`（整份报告作为 md 文档落库 PENDING 待审核，复用 `KbStoreService.createDocument`）
  - `workspace.controller.ts`：新增 `POST /workspace/analyze`（SSE：stage → report_chunk → done{reportId}）、`GET /workspace/reports/:id`、`POST /workspace/reports/:id/save-kb`
  - 模块接线：SearchModule exports GenerateService；WorkspaceModule imports PrismaModule/KbModule/SearchModule
- **前端**：
  - `api/workspace.ts`：`analyzeStream`（POST 读流 + SSE 帧解析）/ `fetchAnalyzeReport` / `saveAnalyzeToKb` + 类型
  - `views/AnalyzeResultView.vue`（独立页 `/workspace/analyze/:id`）：头部信息条 + 5 统计卡片 + MarkdownView 渲染完整 14 章节正文（数据表/来源/可视化章节均在 MarkdownView 内，避免内容重复）+ 下载（导出 Markdown）+ 存入知识库弹窗（库/分组/可见性/标签）
  - `views/WorkspaceView.vue`：工作台「生成分析结果」按钮由占位改为真实触发（组装 AnalyzeParams → SSE 流式 → 进度弹窗 → 跳转分析结果页）；onBeforeUnmount 释放 AbortController
  - `router/index.ts`：注册 `/workspace/analyze/:id`（requiresAuth）
- **Bug 修复（联调发现）**：`components/MarkdownView.vue` 表格分隔行正则 `/^\|?[\s:-]+\|?\s*$/` 无法匹配多列分隔行（如 `| --- | --- |`），导致表格头落入段落循环却因 `TABLE_RE` 排除而 `i` 不递增 → 死循环 + 数组无限增长抛 `RangeError: Invalid array length`。修正为 `/^[\s:|\-]*$/`（分隔行只由 |、空格、冒号、横线组成且含横线）。该 bug 同时影响 M2/M3 多列表格
- **单测**：`analyze.spec.ts`（computeStats/computeTable/buildFallbackReport/14 章节断言）、`workspace.store.service.spec.ts`（saveReport / reportDetail 404 / 字段返回）、`generate.service.spec.ts` 追加 `streamCustom` 用例。全量 29 文件 / 197 用例全绿
- **浏览器联调**（puppeteer-core）：登录态注入 → 工作台加载 8 国 × GDP 增长率 2019-2025 → 点击「生成分析结果」→ SSE 流式（降级生成 3047 字 14 章节）→ 跳转 `/workspace/analyze/:id` → 页面 5 统计卡片 + 3047 字 Markdown 正文 + 数据分析表（MarkdownView 渲染的 markdown 表格）渲染完整 → 存入知识库弹窗正常打开并显示当前租户知识库「M43 Test Lib」
- **API 联调**（curl）：sms/login → `POST /workspace/analyze` SSE 输出 stage(analyzing) → report_chunk(14 章节 3047 字) → stage(done) → done{reportId}；`GET /workspace/reports/:id` 返回完整详情（contentMd/stats/table）；`POST /workspace/reports/:id/save-kb` 创建待审核 md 文档 `{created:1, documents:[{id, name}]}`
- **截图证据**（项目根）：`.m43-1-workspace.png` / `.m43-2-analyze-top.png`（含 14 章节完整渲染）/ `.m43-3-save-dialog.png`（存库弹窗）；联调脚本 `.m43-verify.cjs` / `.m43-verify-save.cjs`
- **构建**：`npx tsc -p tsconfig.build.json --noEmit` 通过；`vue-tsc --noEmit` 通过
- **范围确认决策**：分析输入由前端传合并时序数据（含本地文件，对齐 D1「不落库」）；统计卡片与数据表由后端确定性计算（数字准确 + 降级可渲染）；下载仅导出 Markdown（Word/PPT + 引文映射留 M4.4）；整份报告作为一份 md 文档存库（待审核，对齐原型 `ssWSAnzSaveKb`）
- **遗留提醒（既有 bug，非本次范围）**：`kb.controller.ts` 的 `listLibraries` 用 `findMany({ orderBy, include })` 无显式 `where`，导致 `applyTenantFilter` 不注入 `tenantId`，存在跨租户列表泄漏。`getLibrary`/`listDocuments`/`listGroups` 等通过 `where:{id}` 注入是正确的。建议在 M5 或专项加固中修复（方案：`listLibraries` 改为 `findMany({ where: {}, orderBy, include })` 触发注入，或在 applyTenantFilter 中对无 where 的 findMany 默认注入 `{tenantId}`）

---

### M4.4 阶段记录（2026-08-28）

**M4.4 导出 + 我的数据/我的报告 ✅ 已完成（方案见 docs/11-M4.4技术方案.md，决策 D1/D2/D3 已确认：两类报告都导出、不嵌图、编辑/分享占位）**

- **数据模型**：新增 `workspace_datasets` 表（我的数据 = 收藏的数据集快照：name/data(Json)/tags(Json)/status(ACTIVE·ARCHIVED)/sourceType，migration `workspace_dataset` 已应用）；登记进 `tenant-filter.ts` 的 `TENANT_MODELS`；复用 `workspace_reports` / `search_reports` / `search_sources` 无改动
- **报告导出（新增 report 模块）**：
  - `report/export/markdown.ts`：轻量 Markdown 块级解析纯函数（标题/段落/列表/表格/行内加粗/`{c:N}` 引文），未覆盖结构降级为段落不崩溃
  - `word.builder.ts`：docx 库生成 Word——标题层级 + 正文 + `{c:N}` → 上标 `[N]` + 文末「参考来源」表（按 idx 升序一一对应）
  - `ppt.builder.ts`：pptxgenjs 生成 PPT——封面/摘要/数据表/来源页（D2：不嵌图）
  - `report.service.ts`：`POST /report/export` 编排（type=search 取 sessionId / type=workspace 取 reportId → 归一化 ExportReport → builder → 落审计 EXPORT）；跨租户/不存在 → 404，非法 type/format → 400
  - 新增依赖：`docx` + `pptxgenjs`（纯 JS 无原生编译）
- **我的数据**：`POST /workspace/datasets` 收藏快照（校验 name/data，tags 截断 10 个）/ `GET /workspace/datasets` 列表（keyword/tag/status/分页，DB 过滤 status + 内存过滤 keyword·tag）/ `POST .../:id/archive` 幂等 toggle / `DELETE .../:id` 硬删（跨租户 404）/ `GET .../export` CSV 清单（UTF-8 BOM）
- **我的报告（聚合）**：`GET /workspace/reports` 智搜（session 维度，version=报告数）+ 分析结果报告内存合并按时间降序分页；`GET /workspace/reports/:id/versions`（search 按 session 全部报告升序编号；workspace 单条）
- **前端**：
  - `MyDataView.vue`（/my-data）：搜索/标签/状态筛选 + 表格 + 溯源弹窗（时序快照明细）+ 归档/恢复/删除 + 导出清单
  - `MyReportsView.vue`（/my-reports）：聚合列表（智搜报告/分析报告徽标）+ 浏览（search → 智搜报告页 / workspace → 分析结果页）+ 下载下拉（Word/PPT）+ 版本弹窗；编辑/分享 comingSoon 占位
  - `SearchReportView.vue`（/search/reports/:id）：智搜报告浏览页——MarkdownView 渲染正文（`{c:N}` 角标）+ 来源卡侧栏高亮联动 + 导出文件下拉
  - `AnalyzeResultView.vue`「下载文件」由 Markdown 升级为 Word/PPT 导出下拉；`WorkspaceView.vue`「收藏到我的数据」真实化（组装 indicator/countries/years/series/sources 快照 → POST）；`TopNav.vue` 头像下拉增加「我的数据/我的报告」入口
- **单测**：新增 `markdown.spec` / `word.builder.spec`（`{c:N}` 上标、来源表排序、无引文报告）/ `report.service.spec`（非法 format/type → 400）+ `workspace.store.service.spec` 追加（deleteDataset 跨租户 404 / archiveDataset toggle / listReportVersions 双分支）；后端全量 **240/240 通过**，前后端 build 通过
- **API 联调**（Node 脚本）：智搜 SSE 生成报告（10 来源）→ search 类型导出 docx 11KB（document.xml 含 10 个 superscript 引文上标 ↔ 10 来源）/ pptx 64KB；workspace 类型 docx 20KB / pptx 185KB；版本列表 ✓、非法入参 400 ✓、跨租户 404 ✓、归档双 toggle ✓、CSV 清单中文正常 ✓
- **浏览器联调**（puppeteer-core）：登录态注入 → 工作台收藏 → 我的数据列表 3 条 + 溯源弹窗 8 行时序 + 归档/恢复 → 我的报告 5 条（智搜+分析两类）+ 版本弹窗 → 智搜报告浏览页 10 来源卡 + 正文渲染；全程无 console.error、无 HTTP ≥400
- **截图证据**（项目根）：`.m44-1-workspace.png` / `.m44-2-my-data.png` / `.m44-3-trace.png` / `.m44-4-my-data-archived.png` / `.m44-5-my-reports.png` / `.m44-6-versions.png` / `.m44-7-search-report.png`；联调脚本 `.m44-verify.cjs` / `.m44-api-verify.cjs` / `.m44-api-verify2.cjs`
- **环境备注**：本会话沙箱无全局 puppeteer-core，临时 `npm install --prefix .m44-tools`（验证后已删）；PowerShell `ConvertTo-Json` 显示 Json 数组会失真（`years` 显示为拼接串），以 psql 直查为准（库内为正确数组）
- **M4 里程碑四批次全部完成**，待用户确认后进入 M5 会员计费支付

---

## 五、风险跟踪

| 风险 | 等级 | 缓解措施 | 关联里程碑 |
| --- | --- | --- | --- |
| LLM API 延迟/不稳定 | 高 | 多 Provider 热切换 + 熔断降级 + 缓存兜底 | M2 |
| AnySearch 可用性/额度受限 | 中 | 联网路超时熔断返回空 + SearchConnector 插槽可换供应商 | M2 |
| 大文档切片阻塞队列 | 中 | Worker 横向扩容 + 任务分片 | M3 |
| SSE 长连接占用 | 中 | 网关超时配置 + 心跳 + 连接数上限告警 | M2/M7 |
| 多租户越权 | 高 | ORM 强制注入 + 越权抛异常 + 审计 + 渗透测试 | M1/M7 |
| 计量不准 | 中 | Redis 原子计数 + 定时对账 + 明细可查 | M5 |
