# 14 · M7 上线强化 技术方案

> 版本：v1.0 ｜ 日期：2026-08-31
> 前置：`04-开发规划.md` M7 章节；M2 智搜管道（`07-M3技术方案.md` SSE 五阶段）、M6 系统管理（备份/监控）已就绪
> 原则：沿用项目降级风格——外部依赖（Jaeger/Sentry/LLM）未配置即关闭，不阻塞启动；每批完成停下等用户验收

---

## 一、决策记录（2026-08-31 用户确认）

| 决策点 | 结论 | 理由 |
| --- | --- | --- |
| OTel 导出后端 | docker-compose 加 **Jaeger all-in-one** | 浏览器打开 UI 真实看到管道 span，验收直观 |
| 生产部署形态 | **docker-compose 生产化**（不起 K8s） | 当前单体 NestJS + 4 基础设施，K8s 徒增运维面；真实上云推荐云托管容器（SAE/Fargate）而非自建 K8s，HPA/Ingress 语义在手册中以 Nginx 等价配置说明 |
| 压测工具 | **Node 原生脚本** | 零外部依赖（M6 联调已有 Node 直连 Chrome 先例）；SSE 长连接控制更精细 |
| Sentry | **未配置 DSN 即不初始化** | 沿用支付密钥 mock 降级风格 |

---

## 二、M7.1 可观测四件套

### 2.1 Pino 结构化日志 + requestId 贯穿

**新增依赖**：`nestjs-pino`、`pino`、`pino-http`（dev：`pino-pretty`）。

**设计**：
- `LoggerModule.forRoot` 替换 Nest 默认 Logger；`pino-http` 的 `genReqId` 从请求头 `X-Request-Id` 取（保留网关/上游传入），否则生成 `req-<8位随机>-<毫秒>`，并回写响应头
- **AsyncLocalStorage 请求上下文**（`common/observability/request-context.ts`）：中间件把 requestId + 用户信息存入 ALS，供拦截器、异常过滤器、业务 Service 在任意深度取用；非 HTTP 上下文（BullMQ worker）提供 `runWithRequestId(jobId, fn)` 包裹，worker 日志同样带出 requestId
- 输出：dev 走 pino-pretty（`LOG_PRETTY=true`），默认 JSON 单行；`redact` 屏蔽 authorization/cookie/密码字段
- `AllExceptionsFilter` / `TransformInterceptor` 改用注入的 PinoLogger，日志行统一携带 requestId

### 2.2 Prometheus 指标 + 告警四项

**新增依赖**：`prom-client`。新增 `common/observability/metrics.*`：

| 指标 | 类型 | 标签 | 用途（验收告警项） |
| --- | --- | --- | --- |
| `http_request_duration_seconds` | Histogram | method、route、status | P95/P99 时延 |
| `sse_active_connections` | Gauge | — | SSE 并发数 |
| `sse_connections_total` / `sse_disconnects_total` | Counter | — | SSE 断连率（两者差分） |
| `queue_job_counts` | Gauge | queue、state | BullMQ 队列积压（waiting>阈值告警） |
| `llm_calls_total` | Counter | scene、status | LLM 错误率（error/total 比率） |

- 埋点位置：`MetricsInterceptor`（HTTP 时延，route 取 Controller 路径模板避免高基数）；`search.controller` SSE 流开始/关闭处（SSE 三指标）；`generate.service` / `intent.service` streamText/generateObject 成功失败处（llm_calls_total）；metrics service 内 15s 轮询 `chunk-embed` 队列 `getJobCounts()`（queue_job_counts）
- 暴露：`GET /api/v1/metrics`，`@Public()` + 绕过 TransformInterceptor 原样输出 `text/plain`（生产由 Nginx 限内网访问）
- 告警规则：`infra/prometheus/alerts.yml` 落四条（P95>5s、SSE 断连率>10%、队列 waiting>100、LLM 错误率>20% 5min 窗口），Prometheus 加载

### 2.3 OTel 管道 span

**新增依赖**：`@opentelemetry/api`、`@opentelemetry/sdk-node`、`@opentelemetry/exporter-trace-otlp-http`、`@opentelemetry/resources`、`@opentelemetry/semantic-conventions`。

- `common/observability/otel.ts`：`NodeSDK` 手动初始化，OTLP HTTP 导出到 `OTEL_EXPORTER_OTLP_ENDPOINT`（默认 `http://localhost:4318/v1/traces`）；`OTEL_ENABLED!=='true'` 时整链不启动（api 包 trace.getTracer 退化为 no-op，零开销）
- span 清单：根 span `search.pipeline` 下五段——`search.intent`、`search.fetch.{web,vertical,local}`、`search.fusion`（RRF+重排+去重）、`search.generate`；报告分析 `report.analyze`。requestId 作为 span attribute 关联日志与链路
- docker-compose 加 `jaegertracing/all-in-one`（16686 UI / 4318 OTLP）

### 2.4 Sentry

**新增依赖**：`@sentry/nestjs`。`main.ts` 中 `SENTRY_DSN` 已配置才 `Sentry.init` + `setupNestErrorHandler`；未配置完全跳过（不引 profiling node，保持轻量）。

### 2.5 监控栈 docker-compose 变更

| 服务 | 镜像 | 端口 | 配置 |
| --- | --- | --- | --- |
| jaeger | `jaegertracing/all-in-one:1.62` | 16686 / 4318 | 无需配置文件 |
| prometheus | `prom/prometheus:v2.54.1` | 9091→9090 | `infra/prometheus/prometheus.yml`（抓 API `host.docker.internal:3000`）+ `alerts.yml` |
| grafana | `grafana/grafana:11.2.0` | 3300→3000 | provisioning 自动建 Prometheus 数据源 + 预置 dashboard JSON（四告警面板） |

### 2.6 M7.1 验收

- 单测：requestContext 生成/透传、metrics 计数与 gauge 更新、OTEL 未启用不初始化、Sentry 未配置不初始化；全量回归通过
- curl `/metrics` 出真实计数（先打几个接口再抓）；Grafana 打开 dashboard 见四面板出图；Jaeger UI 见 `search.pipeline` 五段 span
- 日志：JSON 行含 requestId、level、module；异常日志 requestId 与响应头一致

---

## 三、M7.2 压测 + 限流（设计）

- `backend/scripts/bench-sse.mjs`：Node 22 原生 fetch + ReadableStream，并发 N 路 SSE（登录→建流→收五阶段→done/abort），统计成功率/断连率/首包延迟/总耗时
- `backend/scripts/bench-workspace.mjs`：工作台查询接口并发压测，统计 P50/P95/P99
- 限流：`common/rate-limit/` Redis 滑动窗口（`INCR`+`EXPIRE` 原子 Lua），全局中间件按路由组差异化阈值（auth 登录/短信最严，普通读接口宽松，SSE 单用户连接数上限）；配置走 sys_configs 环境变量，压测结果回填阈值
- 基线数据写入本文档阶段记录

## 四、M7.3 安全加固（设计）

- 登录接口防刷（IP+账号双维度，与短信同款 Redis 窗口）
- 敏感词拦截与智搜管道衔接复核（现有 admin 运行监控数据源复用）
- 渗透自测脚本 `backend/scripts/security-audit.mjs`：租户越权矩阵（A 租户 token 访问 B 租户 kb/report/order）、未授权路由扫描、SQL/脚本注入探测样例、支付回调重放（复用 M5 幂等测试）、限流绕过（X-Forwarded-For 伪造）；报告落 `docs/` 阶段记录

## 五、M7.4 备份恢复演练（设计）

- M6.4 备份登记升级真执行：`pg_dump`（docker exec 或 pg 客户端）落 `backups/` 目录 + gzip；保留策略（默认保留 7 份）超量清理
- 恢复演练：脚本起临时库 `ai_research_restore` → `pg_restore`/psql 回灌 → 行数抽查（users/ kb_documents/ orders）→ 结果写 backup_records（type=DRILL）；Qdrant 快照 `POST /collections/{c}/snapshots` 一并落盘
- 演练通过截图/记录进阶段记录

## 六、M7.5 docker-compose 生产化（设计）

- `backend/Dockerfile`、`frontend/{web,admin}/Dockerfile`：多阶段（pnpm fetch → build → 精简运行层，Node 22-alpine）
- `docker-compose.prod.yml`：api ×2 副本（负载均衡由 Nginx upstream 模拟）+ nginx（SSE 透传 `proxy_buffering off`、`proxy_read_timeout` 拉长、HTTPS 自签证书、`/metrics` 与 Grafana 限内网）+ 基础设施 + 监控栈；健康检查/重启策略/内存限制
- 《上线部署手册》（`docs/15-上线部署手册.md`）：环境变量清单、证书、备份恢复操作、HPA/Ingress 的 Nginx 等价语义说明与真实上云路径（推荐 SAE/Fargate）

---

## 七、风险

| 风险 | 缓解 |
| --- | --- |
| OTel SDK 与 Nest/Prisma 兼容性 | 手动 span 不引全量 instrumentation，最小面暴露 |
| /metrics 高基数 | route 用 Controller 路径模板，不落原始 URL |
| 压测打满本机影响 dev | 压测脚本默认限并发/时长，`--duration` 可控 |
| Jaeger/Prometheus 端口冲突 | 与现有 5433/6380 风格一致，映射非常用端口（16686/9091/3300） |

---

## 八、阶段记录

### M7.3 阶段记录（2026-09-02）

**范围**：登录接口防刷（IP+账号双维度）、敏感词拦截复核、渗透自测脚本与实测。

**登录防刷**（提交 2bc35bc）：
- `AuthService.passwordLogin` 在 IP 令牌桶（M7.2）之外增加账号（phone）维度失败计数：连续失败 5 次锁定 15 分钟（`DEFAULT_LOGIN_POLICY` 可注入），成功后清零；用户不存在同样计失败，防手机号枚举；复用 `SmsKvStore` 抽象（Redis 固定窗口）
- 新增错误码 `LOGIN_LOCKED=1010`；单测 `login-guard.spec` 3 项

**敏感词拦截（D8）复核**：`checkSensitiveWord` 仍在智搜 `setHeader`（首包回写）之前执行，拦截时序无回归，无需改动。

**渗透自测脚本**（`backend/scripts/security-audit.mjs`，零依赖）：未授权路由扫描、租户越权矩阵（直读+列表）、SQL/脚本注入探测、支付回调重放（下单→mock-pay→重放→订阅到期时间比对）、X-Forwarded-For 限流绕过；`npm run bench:audit` 运行。

**实测发现并修复两个安全缺陷**（2026-09-02）：
1. **租户列表泄漏（高危）**：`tenant-filter.ts` 的 `applyTenantFilter` 原来只在 `where !== undefined` 时注入 `tenantId`，`findMany` 不带 where（如知识库列表）退化为全表查询，B 租户可在列表中看到 A 租户的 PRIVATE 库（直读详情正常 404）。修复：WHERE 类操作一律注入（where 缺省时创建 `{ tenantId }`）；`tenant-filter.spec` 补 2 项用例
2. **限流路由分组失配（中危）**：`rate-limit.guard.ts` 用 `req.route.path`（含全局前缀 `/api/v1`）做 `startsWith('/auth')` 匹配，永远失配——auth 组从未走最严限流、`/metrics` 豁免也失效，全部落入宽松 global 桶。修复：`resolveGroupPath` 组装 `baseUrl + route.path` 并剥离 `/api/v1` 前缀后再分组；`rate-limit.spec` 补带前缀用例（修复前 40 次伪造 XFF 登录不触发 429，修复后正确触发）
3. 支付重放检查由占位 SKIP 补为真实实现（下单→首付→重放→到期时间不二次推进）

**验收结果**：本地起 postgres/redis + dev 服务后实测渗透自测 **PASS 16 / FAIL 0 / WARN 0 / SKIP 0**（未授权扫描 8 项、租户越权 2 项、注入探测 4 项、支付重放 1 项、限流绕过 1 项）；全量后端回归 58 文件 489 测试通过；eslint 通过。
